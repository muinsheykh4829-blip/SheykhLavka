name: picker_app
description: "Приложение для сборщика заказов Sheykh Lavka"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.5.1'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.0.5
  shared_preferences: ^2.5.3
  flutter_ringtone_player: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
